fastapi>=0.104.0
uvicorn>=0.24.0
toml>=0.10.2
google-genai>=1.0.0
groq>=0.4.0
streamlit>=1.28.0
python-dotenv>=1.0.0

# ML detector (Layer 2, Stage 1). Optional at runtime — ml_detector.py fails
# open if these are absent, and detection falls back to regex plus the LLM.
# Adds roughly 60 MB to a deployment via numpy/scipy.
scikit-learn>=1.9.0      # match the version models/detector.joblib was pickled with
joblib>=1.3.0

# Fine-tuned transformer detector (Stage 2) — runs models/transformer/*.onnx.
# ~80 MB installed; PyTorch is NOT needed at runtime. Optional like the rest:
# without these, ml_detector.py fails open.
onnxruntime>=1.18.0
tokenizers>=0.19.0
